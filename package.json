{
  "name": "tictactoe",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "start": "vite",
    "preview": "vite preview",
    "build": "npm run clear && vite build",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write src/**/*.js",
    "format:lint": "npm run fromat && npm run lint",
    "clear": "rimraf dist .parcel-cache"
  },
  "dependencies": {
    "assets": "^3.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "babel-preset-nano-react-app": "^0.1.0",
    "vite": "^5.0.10"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ]
  }
}
